# P1: types

## Goal

Answer the linearity question while it is cheap: can affine/linear tiles
express the kernels inference needs without fighting the author? The roadmap
moves this forward from the design doc's M0 because hand-typing kernels on
paper always looks fine. A checker and an interpreter running a real
flash-attention decode loop are what answer it.

## Exit test

Flash-attention **decode** inner loop — K reuse across query blocks,
double-buffered — type-checks and matches PyTorch in the interpreter. Its
Hopper schedule type-checks too.

## Status

**Exit test met in the interpreter.** Both halves are covered: the
double-buffered decode loop, and the Hopper schedule in its real
warp-specialised shape. Everything lives in `crates/lex-front`.

| Piece | State |
| --- | --- |
| Tile types: dtype, shape, space (`Global`/`Threadgroup`/`Reg`) | done |
| Linearity: use-after-move, leaks, region-scoped borrows | done |
| Effects: `copy_async` → `Future`, `wait` discharges | done |
| Loop-invariant carries, affine view bounds, budget | done |
| Reference interpreter (f32 math, dtype rounding at tile boundaries) | done |
| NVIDIA Hopper and AMD CDNA3 target rows | done |
| Flash decode matches PyTorch, Metal and Hopper schedules | done |
| Barrier-synchronised pipes; warp-specialised producer/consumer roles | done |
| Barrier arrival counts derived from types; deadlock-freedom checked | done |
| Concurrent interpreter (roles on threads, deadlock detection) | done |
| Layout in the type (`Swizzle`, `MmaFrag`) | not started |
| Surface syntax / parser | not started |

```sh
cargo test -p lex-front
cargo run -p lex-front --example flash_decode               # verdicts per target
cargo run -p lex-front --example flash_decode -- --ir ws    # the program (metal|hopper|ws)
python3 scripts/flash_decode_golden.py                       # regenerate the golden
```

## The test case

32 query heads sharing one KV head (MQA decode), head dim 128, 512 KV
positions, f16 storage, f32 accumulation. The KV loop is outermost and the
query-block loop innermost, so each K/V tile is fetched once and read by every
query block — the order that stresses linearity:

```text
for each KV block, pipelined `stages` deep:
    issue copy of block i + stages-1 into the free buffer
    K, V = wait(oldest copy)
    for each query block:              # K, V borrowed by every iteration
        online-softmax update of (m, l, acc)[qb]
    K, V become the free buffers
```

The schedule — `bq`, `bk`, `stages`, where K/V are staged — is a builder
parameter. One function produces every variant; the checker decides where
each fits:

| Schedule | Threadgroup peak | apple-m-series (32 KiB) | nvidia-hopper (227 KiB) | amd-cdna3 (64 KiB) |
| --- | --- | --- | --- | --- |
| bq 8, bk 32, 2 stages | 32 768 B | ok, warns: no async copy | ok | ok, warns |
| bq 16, bk 128, 3 stages | 196 608 B | **rejected**: budget | ok | **rejected**: budget |
| same, producer warp + 2 consumer warpgroups | 196 608 B | **rejected**: no split barriers | ok | **rejected**: no split barriers |

Interpreter against the f64 reference: max relative error 1.0e-5 (Metal
schedule) and 1.8e-5 (both Hopper schedules). The f64 reference agrees with PyTorch's
`scaled_dot_product_attention` to under 1e-6, and both interpreter runs are
also checked directly against the PyTorch output at 1e-4.

## Findings

**K reuse needed no `dup`.** Zero dups in every schedule. A loop body may
borrow anything in scope and may only consume what it defines or is carried —
Rust's closure-capture rule, lexically scoped. That rule alone made "K read by
every query block" ordinary code. The design doc's fallback (explicit `dup`
plus borrow views) turned out to need only the second half.

**Per-query-block state needed a new region form.** The accumulators
`(m, l, acc)` for each query block must survive across outer iterations. An
indexed `take(arr, qb)` / `put(arr, qb, x)` needs a proof that slots are
disjoint, which a simple checker cannot give. `map_each` — move element *i* in,
yield its replacement — makes it structurally obvious. Cost: the state is three
parallel arrays, because there are no tuples of tiles yet.

**Double buffering is a typed loop carry.** The carry is `stages-1` futures
plus one free buffer, and it must have the same type every iteration. The
classic pipelining bug — prefetching into the buffer still being read — is a
use-after-move, because issuing a copy moves its destination. Prefetching past
the end of the sequence is a bounds error, caught from the loop range.

**Futures pin memory.** An in-flight copy holds its destination buffer, so the
budget counts futures. The Metal schedule fills exactly 32 KiB; one more stage
or a wider block is a compile error, not a pipeline-creation failure.

**Where it was awkward, it was not linearity.** Two things cost effort, and
both are structure: the prologue/epilogue has to be peeled (the IR has no
`Option<Future>`, so the last `stages-1` iterations are emitted separately),
and the loop carries seven values. Both were written by a Rust builder
function, which is precisely the "double-buffering is a compiler transform"
the design doc wants — here it is the transform's *output* that is checked, and
that looks like the right split: the schedule generates typed IR, the checker
verifies it.

## Warp specialisation

The Hopper schedule's real shape is FlashAttention-3's. One producer warp
issues TMA copies into a 3-slot ring. Two consumer warpgroups each own half the
query blocks and read every slot. Every slot has a *full* mbarrier and an
*empty* one.

```text
specialize {
  pipe prod -> [cons0, cons1]: (K[128,128], V[128,128]) x3
  role producer x1 warp:   for i: slot = acquire prod; commit prod [k_i, v_i] -> slot
  role consumer x4 warps:  for i: kv = receive cons; attend(&kv.0, &kv.1); release cons, kv
  ...
}
```

The expected risk was that single-owner futures could not describe one tile
read by several warpgroups at once. It resolved without changing the tile type
system. The fix was to put the linearity on the *protocol tokens* instead of on
the data:

- `acquire` returns a `Slot`: write-only, and consumed by `commit`.
- `receive` returns a `Share`: read-only, its parts only borrowable, and
  consumed by `release`. Dropping either one is rejected, because it would
  never arrive on its barrier.
- Each consumer end of a pipe is a linear handle moved into exactly one role.
  So the empty barrier's arrival count equals the number of handles, and it is
  **derived from the type, never written by hand**. The checker reports it:
  full = 1 arrival + 64 KiB of transaction bytes, empty = 2 arrivals.

Deadlock-freedom on a pipe is checked statically, from two facts. First, every
consumer receives exactly as many times as the producer commits; trip counts
are static, so this is counted exactly. Second, no role waits on a pipe while
holding `stages` of its tokens. The bugs that remain expressible — a short
consumer loop, a share held across the whole ring — are rejected with a
`Protocol` diagnostic.

The interpreter runs roles on real threads, so every run is a different
interleaving. Pipes block exactly as barriers do, and a wait that cannot finish
is reported as a deadlock rather than hanging. The warp-specialised decode
matches PyTorch, and it gives bit-identical output across 20 interleavings per
test run.

Targets without split barriers (Metal, CDNA3) reject the region outright. There
is no lowering that is not a spin loop, and the design doc's rule is to reject
rather than silently degrade.

**Verdict:** linear tiles do not make flash-attention decode awkward, either
pipelined or warp-specialised. The design-doc risk "linearity makes common
kernels awkward" did not materialise for attention. What it cost was two
structured forms, `map_each` for per-block state and `specialize` for roles,
and no escape hatches.

## Not answered yet

- **Cross-pipe deadlock.** Deadlock-freedom is proved per pipe. Two pipes
  consumed in opposite orders by two roles could still deadlock; decode
  bundles K and V into one pipe, so it never does. A global wait-order check
  is needed before a kernel uses several pipes.
- **Producer/consumer asymmetry.** One producer per pipe. Split-K style
  multi-producer reductions are not expressible.
- **GEMM A-fragment reuse across the N loop**, and the **persistent-megakernel
  arena** reused across phases with different types — the other two stress
  cases the roadmap names.
- **Layouts.** Every tile is row-major; there is nothing to check a swizzle or
  an MMA fragment against yet.
- **Uninitialised reads** of an `alloc` are caught by the interpreter, not the
  checker (a typestate question).
- **Register pressure** is not budgeted. Staging all 512 K/V rows in registers
  type-checks; it would not run well.
