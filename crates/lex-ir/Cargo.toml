[package]
name = "lex-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Typed tile IR and CPU reference interpreter for the tile language."

[dependencies]
half = { workspace = true }
